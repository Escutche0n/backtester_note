# Agent Workflow — DEPRECATED

> 本文件已作废。当前协作协议见 [AGENTS.md](../../AGENTS.md)。
>
> 旧版采用 GPT=dev / Opus=review 轮转模式；新版改为
> **Opus=iOS 客户端 / GPT=后端与契约层** 的并行分治。
>
> 历史内容保留在 git history 里：`git log -p docs/collaboration/AGENT_WORKFLOW.md`。
